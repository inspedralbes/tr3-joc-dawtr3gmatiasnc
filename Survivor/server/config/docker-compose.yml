services:
  trainer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mlagents-trainer
    working_dir: /workspace
    ports:
      - "5004:5004"
    volumes:
      - .:/workspace
    tty: true
    stdin_open: true